//Majority Element
//Brute Force Approach
//Time Complexity-O(nlogn+n)=O(nlogn)
//Space Complexity-O(logn)
class Solution {
    public int majorityElement(int[] nums) {
        Arrays.sort(nums);
        int i=0,count=1,j=i+1,ele=0,max=0;
        if(nums.length==1) return nums[0];
        while(i<nums.length-1)
        {
            j=i+1;
            count=1;
            while(j<nums.length && nums[i]==nums[j])
            {
                count++;
                j++;
            }
            if(count>max)
            {
                max=count;
                ele=nums[i];
            }
            i=j;
        }
        return ele;
    }
}


//Majority Element
//Better Approach
//Time Complexity-O(n)
//Space Complexity-O(n)
class Solution {
    public int majorityElement(int[] nums) {
        int n=nums.length;
       HashMap<Integer,Integer> map=new HashMap<>();
       for(int num:nums)
       {
        map.put(num,map.getOrDefault(num,0)+1);
       }
       for(Map.Entry<Integer,Integer>entry:map.entrySet())
       {
        if(entry.getValue()>n/2)
        {
            return entry.getKey();
        }
       }
       return -1;
    }
}

//Majority Element
//Optimal Approach
//Moores Voting Algorithm
//Time Complexity-O(n)
//Space Complexity-O(1)
class Solution {
    public int majorityElement(int[] nums) {
        int ele=0,cnt=0;
       for(int i=0;i<nums.length;i++)
       {
        if(cnt==0)
        {
            ele=nums[i];
            cnt=1;
        }
        else if(ele==nums[i])
        {
            cnt++;
        }
        else
        {
            cnt--;
        }
       }
       return ele;
    }
}

//Maximum Subarray
//Brute Force Apporach
//Time Complexity-O(n*n)
//Space Complexity-O(1)
class Solution {
    public int maxSubArray(int[] nums) {
        int max=Integer.MIN_VALUE;
        for(int i=0;i<nums.length;i++)
        {
            int sum=0;
            for(int j=i;j<nums.length;j++)
            {
                sum+=nums[j];
                if(sum>max)
                {
                    max=sum;
                }
            }
        }
        return max;
    }
}


//Maximum Subarray
//Optimal Apporach
//Kadanes Algorithm
//Time Complexity-O(n)
//Space Complexity-O(1)
class Solution {
    public int maxSubArray(int[] nums) {
        int max=Integer.MIN_VALUE,sum=0;
        for(int i=0;i<nums.length;i++)
        {
            sum+=nums[i];
            max=Math.max(sum,max);
            if(sum<0)
            {
                sum=0;
            }
        }
        return max;
    }
}

//Maximum Subarray extended version to print entire Subarray
//Optimal Apporach
//Kadanes Algorithm
//Time Complexity-O(n)
//Space Complexity-O(1)
class Solution {
    public int maxSubArray(int[] nums) {
        int max=Integer.MIN_VALUE,sum=0,start=0,end=0,ansstart=0;
        for(int i=0;i<nums.length;i++)
        {
            if(sum==0)
            {
                start=i;
            }
            sum+=nums[i];
            if(sum>max)
            {
                max=sum;
                ansstart=start;
                end=i;
            }
            if(sum<0)
            {
                sum=0;
            }
        }
        for(int i=ansstart;i<=end;i++)
        {
            System.out.println(nums[i]);
        }
        return max;
    }
}

//Stock Buy and Sell
//Brute force approach
//Time Complexity-O(n*n)
//Space Complexity-O(1)
class Solution {
    public int maxProfit(int[] nums) {
        int profit=Integer.MIN_VALUE,maxprofit=0;
        for(int i=0;i<nums.length;i++)
        {
            for(int j=i+1;j<nums.length;j++)
            {
                if(nums[i]>nums[j])
                continue;
                profit=Math.abs(nums[i]-nums[j]);
                if(profit>maxprofit)
                {
                    maxprofit=profit;
                }
            }
        }
        return maxprofit;
    }
}

//Stock Buy and Sell
//BOptimal approach
//Time Complexity-O(n)
//Space Complexity-O(1)
class Solution {
    public int maxProfit(int[] nums) {
        int profit=0,minprice=Integer.MAX_VALUE;
        for(int i=1;i<nums.length;i++)
        {
          minprice=Math.min(minprice,nums[i-1]);
          profit=Math.max(profit,nums[i]-minprice);
        }
        return profit;
    }
}

//Rearrange Array ELements by sign
//Brute Force Approach
//Time Complexity-O(n)
//Space Complexity-O(n)
class Solution {
    public int[] rearrangeArray(int[] nums) {
        int[] pos=new int[nums.length/2];
        int[] neg=new int[nums.length/2];
        int p=0,n=0;
        for(int i=0;i<nums.length;i++)
        {
            if(nums[i]>0)
            pos[p++]=nums[i];
            else
            neg[n++]=nums[i];
        }
        p=0;n=0;
        for(int i=0;i<nums.length/2;i++)
        {
            nums[2*i]=pos[p++];
            nums[2*i+1]=neg[n++];
        }
        return nums;
    }
}
